import React from 'react';

interface Props {
    options: object,
}

const SelectOptions = ( { options } : Props ) => {
  return (
    <div className="app">
      test
    </div>
  );
}

export default SelectOptions;
